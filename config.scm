(declare (unit config))

(define cfg-name "pigmanclassic test server")	; the name of the server (max length 64 characters)
(define cfg-motd "welcome!")			; the motd (max. 64 characters)
(define cfg-port 25565)				; the port to use
